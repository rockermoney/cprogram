#include<stdio.h>
int main(){
int a,b,c,sum,cube,n;
sum=a*100+b*10+c*1;
cube=a*a*a+b*b*b+c*c*c;printf("enter the number is:");
scanf("%d",&n);
if(sum==cube)
printf("yes");
else
printf("no");
return 0;
}
